package com.robot.slam

object SlamConfig {
    /** ARCore projection near plane in metres. */
    const val PROJ_NEAR_M = 0.1f

    /** ARCore projection far plane in metres. */
    const val PROJ_FAR_M = 20f
}
